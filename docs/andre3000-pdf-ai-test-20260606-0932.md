# Andre3000 PDF AI Write Test

**Datum:** 2026-06-06  
**Run-ID:** 20260606T072959Z  
**Testzeit (UTC):** 07:32:19 – 07:32:21 (0,92 Sekunden)  
**Modus:** classify (kein dry_run, kein force)  
**Prefix:** `_root_part000/102129`  
**Max-Files:** 1  
**Erstellt von:** Andre3000 AI-Agent  

---

## 1. Executive Summary

| Prüfpunkt | Ergebnis |
|-----------|----------|
| PDF gefunden | ✅ `_root_part000/102129.pdf` (96.927 Bytes) |
| PyMuPDF verwendet | ✅ `extraction_method_counts: "pymupdf:1"` |
| Text extrahiert | ✅ 4.000 chars (max-Limit AI_MAX_CHARS_PER_FILE) |
| AI aufgerufen | ✅ Groq / llama-3.3-70b-versatile |
| AI-Ergebnis plausibel | ✅ `class=finance`, `conf=90` |
| Tags in Azure geschrieben | ✅ `dry_run=false` |
| Reports hochgeladen | ✅ 10 Dateien |
| Fehler | ✅ 0 |
| budget_exhausted | ✅ 0 |
| needs_ai nach Test | ✅ 0 |

**Gesamtstatus: GRÜN**  
Die vollständige PDF-Pipeline (Azure → Download → PyMuPDF-Extraktion → Groq-KI → Tag-Write → Report-Upload) funktioniert fehlerfrei. PyMuPDF wurde erstmals produktiv verwendet.

---

## 2. Ausgangslage

Nach dem erfolgreichen needs_ai-Retry-Test (Run `20260606T072012Z`) waren die 3 bekannten PDFs im Container `cool-stage-test` noch ungetaggt (`status=NO_STATUS`):

```
_root_part000/102129.pdf    (96.927 Bytes)
_root_part000/1133248.pdf
_root_part000/1235690.pdf
```

Dieser Test adressiert gezielt die erste PDF-Datei. Die anderen beiden bleiben für spätere Läufe.

---

## 3. Technische Prüfung

### PDF gefunden

```json
{"event": "blob_seen", "blob_name": "_root_part000/102129.pdf", "size_bytes": 96927}
{"event": "blob_detected_untagged", "reason": "status=none"}
```

✅ Datei korrekt als ungetaggt erkannt.

### Extraktion via PyMuPDF

```json
{"event": "ai_called", "input_chars": 2000, "provider": "groq"}
```

```
extraction_success_count: 1
extracted_chars_total:    4000
extraction_method_counts: "pymupdf:1"
```

✅ PyMuPDF hat aus der PDF-Datei 4.000 Zeichen extrahiert (auf `AI_MAX_CHARS_PER_FILE=2000` für den AI-Call gekürzt). Der Extraktor funktioniert in-memory (kein Dateisystem-Write).

### AI-Klassifikation

```json
{"event": "ai_result_validated", "class_label": "finance", "confidence": "90"}
{"event": "blob_classified", "class_label": "finance", "confidence": "90",
 "reason_code": "no_rule_match", "dry_run": false, "duration_ms": 815}
```

✅ Groq hat `class=finance`, `conf=90` zurückgegeben. Plausibel für eine Finanzdatei.

### Tags & Metadata in Azure

`dry_run=false` und `AI_WRITE_TAGS=true`:

| Tag | Wert |
|-----|------|
| `status` | `classified` |
| `class` | `finance` |
| `confidence` | `90` |
| `llm_used` | `true` |
| `needs_ai` | `false` |
| `dsgvo` | (vom AI gesetzt) |
| `archive_candidate` | (vom AI gesetzt) |
| `readable` | `true` |

✅ Tags wurden in Azure Blob Storage geschrieben.

### Reports

```
Container: reports
Prefix:    pilot-v0.1/20260606T072959Z
Dateien:   10
```

✅ Alle 10 Standard-Reports hochgeladen.

---

## 4. Token-Report

| Metrik | Wert |
|--------|------|
| Modell | `llama-3.3-70b-versatile` |
| Prompt-Tokens (real) | 1.069 |
| Completion-Tokens (real) | 74 |
| Gesamt-Tokens (real) | **1.143** |
| Estimated Raw | 779 |
| Estimated Buffered (×1.4) | 1.091 |
| Token-Quelle | `provider_usage:1` |
| Abweichung Real vs. Raw | +46,7% |
| Buffered vs. Real | 1.091 vs. 1.143 → −4,5% (knapp unter Real) |
| AI-Latenz | 424 ms |

> **Hinweis zur Abweichung:** Bei der PDF waren die echten Tokens (+46,7% über raw) leicht höher als der Sicherheitspuffer (×1.4). Dies deutet darauf hin, dass PDF-Inhalte komplexere Tokens erzeugen (ggf. Sonderzeichen, Zahlen, Formatierungen aus der Finanzdatei). Der Safety Factor 1.4 liegt knapp unter der realen Abweichung. Für PDF-lastige Läufe könnte 1.5 sinnvoll sein.

---

## 5. Performance

| Metrik | Wert |
|--------|------|
| Gesamtdauer | 0,92 Sekunden |
| Durchsatz | 3.905 Dateien/Stunde |
| Bytes verarbeitet | 96.927 |
| AI-Latenz | 424 ms |

---

## 6. Fachliche Bewertung

| Aspekt | Bewertung |
|--------|-----------|
| Klasse `finance` für `102129.pdf` | Plausibel – numerische Dateinamen in `_root_part000/` sind typischerweise GEMA-Abrechnungsdokumente |
| Konfidenz 90 | Hoch – PDF-Text war ausreichend klar |
| PyMuPDF in-memory | Sicherheitsprinzip eingehalten (kein Tempfile für PDFs) |
| 1 AI-Call für 1 PDF | Budget korrekt verwaltet |

---

## 7. Vergleich: .doc (antiword) vs. .pdf (PyMuPDF)

| Metrik | .doc (antiword) | .pdf (PyMuPDF) |
|--------|-----------------|----------------|
| Extraktor | antiword (subprocess) | fitz in-memory |
| Extracted chars | ~850–2.000 | 4.000 (max) |
| AI input chars | 858–2.000 | 2.000 (gecapped) |
| AI-Latenz | 295–446 ms | 424 ms |
| Token-Abweichung (raw vs. real) | +34–36% | +46,7% |
| Safety Factor reicht? | ✅ Ja (1.4) | ⚠️ Knapp (1.091 vs. 1.143) |

---

## 8. Bekannte PDFs – Status nach Test

| Datei | Größe | Status |
|-------|-------|--------|
| `_root_part000/102129.pdf` | 96.927 B | ✅ `classified` (finance, conf=90, llm_used=true) |
| `_root_part000/1133248.pdf` | unbekannt | ⏳ noch ungetaggt |
| `_root_part000/1235690.pdf` | unbekannt | ⏳ noch ungetaggt |

---

## 9. Empfehlung nächster Schritt

| Priorität | Maßnahme |
|-----------|----------|
| 1 | Zweite PDF testen: `docker compose run --rm worker --mode classify --prefix "_root_part000/1133248" --max-files 1` |
| 2 | Dritte PDF: `--prefix "_root_part000/1235690" --max-files 1` |
| 3 | Alle 3 PDFs in einem Lauf: `--prefix "_root_part000/1" --max-files 3` (enthält auch .doc-Dateien mit `1...`) |
| 4 | Safety Factor für PDF-Läufe auf 1.5 erhöhen (`AI_TOKEN_ESTIMATION_SAFETY_FACTOR=1.5`) |
| 5 | Größeren Lauf: `--max-files 50` (bereits klassifizierte Dateien werden übersprungen) |

**Noch nicht ausgeführt.** Warte auf explizite Freigabe.

---

## 10. Anhang

### 10.1 Run-Finished Log

```json
{
  "run_id": "20260606T072959Z",
  "status": "ok",
  "files_processed": 1,
  "files_classified": 1,
  "files_unknown": 0,
  "ai_calls_used": 1,
  "ai_total_tokens": 1143,
  "ai_estimated_tokens_buffered_total": 1091,
  "ai_token_estimation_safety_factor": 1.4,
  "extraction_method_counts": "pymupdf:1",
  "extracted_chars_total": 4000,
  "llm_used_count": 1,
  "needs_ai_count": 0,
  "retry_recommended_count": 0,
  "reports_uploaded": true
}
```

### 10.2 Umgebungsvariablen (sensitiv redaktiert)

```
ENABLE_AI=true
AI_PROVIDER=groq
AI_MODEL=llama-3.3-70b-versatile
AI_WRITE_TAGS=true
AI_MAX_CALLS_PER_RUN=10
AI_MAX_CHARS_PER_FILE=2000
AI_TOKEN_ESTIMATION_SAFETY_FACTOR=1.4
PDF_MAX_PAGES=3
GROQ_API_KEY=***REDACTED***
AUTH_MODE=device_code
AZURE_STORAGE_ACCOUNT=stgemaclasspilot001
```
